% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loopQTLsetup.R
\name{loopQTLsetup}
\alias{loopQTLsetup}
\title{Validate inputs and build loopQTL object}
\usage{
loopQTLsetup(
  samples,
  vcf,
  resolution,
  minSamples = 2,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{samples}{a data.frame with columns \code{Sample}, \code{hic}, \code{loops}.
Optional \code{cis} column. If the \code{cis} column is absent, it is calculated
from the \code{.hic} files automatically.}

\item{vcf}{Path to a VCF. The vcf should be bgzipped with tabix index.}

\item{resolution}{a numeric bin size recognized in the .hic file (e.g. 10000).}

\item{minSamples}{The minimum number of samples supporting a consensus
loop. Default is 2 samples.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam-class]{BiocParallel::BiocParallelParam} that parallelizes the
cis-computing straw pulls.}
}
\value{
A \linkS4class{loopQTL} object with \code{samples}, \code{loops}, \code{vcfPath},
\code{resolution}, \code{cis}, and \code{metadata} populated.
\code{phenotype} is an empty placeholder until loopQTLnormalize() fills it.
}
\description{
\itemize{
\item validates the input files header, including .vcf and .hic files;
\item reads each per-sample BEDPE and builds a consensus loop set by
exact-coordinate matching. Loops found fewer than \code{minSamples} samples will be
dropped;
\item Automatically computes per-sample cis-contact depth from the \code{.hic} files
when the samplesheet lacks a \code{cis} column.
}
}
\details{
The samplesheet is a tab-separated file with columns \code{Sample}, \code{hic},
\code{loops}, and optionally \code{cis}.
}
\examples{
## Get the necessary input example files path.
extdataDir <- system.file("extdata", package = "loopQTL")
vcfPath <- file.path(extdataDir, "example.vcf.gz")
samplesheetPath <- file.path(extdataDir, "example_samplesheet.tsv")
samples <- read.table(samplesheetPath, header = TRUE)

##modify the relative file path
samples$hic <- unlist(
    lapply(samples$hic, function(x) {
        file.path(extdataDir, x)
    }))
samples$loops <- unlist(
    lapply(samples$loops, function(x) {
        file.path(extdataDir, x)
    }))

## Run loopQTLsetup
obj <- loopQTLsetup(
    samples    = samples,
    vcf        = vcfPath,
    resolution = 10000,
    minSamples = 2)
}
